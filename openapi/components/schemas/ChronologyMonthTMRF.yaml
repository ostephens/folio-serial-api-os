type: object
required:
  - monthFormat
  - yearFormat
properties: 
  monthFormat:
    oneOf: 
      -  type: string
      - $ref: RefData.yaml
  yearFormat:
    oneOf:
      - type: string
      - $ref: RefData.yaml
